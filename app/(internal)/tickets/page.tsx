import { ScreenStub } from "@/components/shell/screen-stub";

/** Registered as `queue`; built in P1.5.5. */
export default function QueuePage() {
  return <ScreenStub screen="queue" item="P1.5.5" />;
}
