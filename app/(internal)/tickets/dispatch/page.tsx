import { ScreenStub } from "@/components/shell/screen-stub";

/** Registered as `dispatch`; built in P2.12.4. */
export default function DispatchPage() {
  return <ScreenStub screen="dispatch" item="P2.12.4" />;
}
