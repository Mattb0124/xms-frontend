import { ScreenStub } from "@/components/shell/screen-stub";

/** Registered as `ticket_new`; built in P1.5.5. */
export default function TicketNewPage() {
  return <ScreenStub screen="ticket_new" item="P1.5.5" />;
}
