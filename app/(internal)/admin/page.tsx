import { ScreenStub } from "@/components/shell/screen-stub";

/** Registered as `admin`; built in P1.4.3. */
export default function AdminPage() {
  return <ScreenStub screen="admin" item="P1.4.3" />;
}
