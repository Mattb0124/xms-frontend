import { ScreenStub } from "@/components/shell/screen-stub";

/** Registered as `timesheet`; built in P2.12.2. */
export default function TimesheetPage() {
  return <ScreenStub screen="timesheet" item="P2.12.2" />;
}
