import { SearchHome } from "@/components/portal/search-home";

/** Client portal home (portal.<domain>): search first, then the two quiet cards. */
export default function PortalHomePage() {
  return <SearchHome />;
}
